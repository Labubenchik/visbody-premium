---

---

<div class="wrap">
	<div class="head">
		<div class="eyebrow">Hardware Ecosystem</div>
		<h1 class="title">
			Системы{" "}
			<span class="title-accent"> Visbody </span>
		</h1>
		<p class="subtitle">Профессиональные решения для анализа состава тела и 3D-антропометрии</p>
	</div>
</div>

<style>
	.wrap {
		position: relative;
		isolation: isolate;
		padding: 78px 24px 64px;
	}

	.head {
		position: relative;
		text-align: center;
	}

	.eyebrow {
		display: inline-block;
		margin-bottom: 24px;
	}

	.title {
		margin: 0 0 24px;
		color: #ffffff;
		font-family: var(--font-sans);
		font-size: 48px;
		font-weight: 800;
		line-height: 1;
		letter-spacing: -0.05em;
		text-transform: uppercase;
	}

	.title-accent {
		letter-spacing: -0.02em;
	}

	.subtitle {
		max-width: 672px;
		margin: 0 auto;
		color: #cbd5e1;
		font-family: var(--font-sans);
		font-size: 18px;
		line-height: 28px;
		letter-spacing: -0.025em;
		opacity: 0.8;
	}

	@media (min-width: 768px) {
		.title {
			font-size: 96px;
		}
	}
</style>
